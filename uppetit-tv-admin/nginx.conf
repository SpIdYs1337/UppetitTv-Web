server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Главная магия: если файла или папки нет, отдаем index.html
        try_files $uri $uri/ /index.html;
    }
}